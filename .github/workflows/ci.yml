name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  check:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ./app

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v4

      - name: Install ktfmt
        run: |
          KTFMT_VERSION="0.61"
          curl -sSLO "https://repo1.maven.org/maven2/com/facebook/ktfmt/${KTFMT_VERSION}/ktfmt-${KTFMT_VERSION}-with-dependencies.jar"
          echo '#!/bin/bash' > ktfmt
          echo "java -jar $(pwd)/ktfmt-${KTFMT_VERSION}-with-dependencies.jar \"\$@\"" >> ktfmt
          chmod +x ktfmt
          sudo mv ktfmt /usr/local/bin/

      - name: Check formatting
        run: ktfmt -n --set-exit-if-changed .

      - name: Run detekt
        run: ./gradlew detekt

      - name: Run tests
        run: ./gradlew :app:test --scan
        - name: Upload APK
  uses: actions/upload-artifact@v4
  with:
    name: app-debug-apk
    path: app/build/outputs/apk/debug/*.apk
